import 'package:flutter/material.dart';

class StatelessPage extends StatelessWidget {
  const StatelessPage(
      {super.key, String this.name, String this.location, int this.phone});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
